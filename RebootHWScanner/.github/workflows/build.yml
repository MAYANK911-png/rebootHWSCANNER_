name: Build Debug APK

on:
  push:
    branches: [ "**" ] # Triggers on ANY branch to ensure detection
  workflow_dispatch:   # Enables the manual "Run" button

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Hello World
        run: echo "Workflow detected successfully!"   
